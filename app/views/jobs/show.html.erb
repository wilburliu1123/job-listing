<div class="jumbotron">
  <h3 class="title">
    只有想不到，没有找不到。
  </h3>
</div>
  <h2><%= @job.title %> </h2>
  <p><%= @job.description %> </p>
  <%= link_to("投简历", new_job_resume_path(@job), class: "btn btn-default ")%>
</div>
